{
  "name": "supply-chain-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Supply Chain Tracker - Blockchain DApp",
  "scripts": {
    "prepare": "husky",
    "test": "cd sc && forge test",
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "lint": "cd web && npm run lint",
    "format": "cd web && npm run format"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11"
  }
}
